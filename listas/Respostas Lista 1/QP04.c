#include <stdio.h>

int main(){
    int quantidadeDeDias = 10;

    int quantidadeDeHoras = quantidadeDeDias * 24;

    printf("A quantidade de horas em %d dias eh: %d\n", quantidadeDeDias, quantidadeDeHoras);
    return 0;
}