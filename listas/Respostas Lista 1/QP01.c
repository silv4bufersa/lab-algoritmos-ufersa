#include <stdio.h>

int main(){
    // declarando as variáveis para os pesos já com os valores
    int peso1 =5;
    int peso2 =10;
    int peso3 =15;

    // somando e imprimindo o peso total (usando eh pois o meu terminal nao aceita os acentos)
    printf("O peso total eh: %d\n", peso1+peso2+peso3);
    return 0;
}