#include <stdio.h>
#include <locale.h>

int main(void)
{
    int idade = 18;
    if (idade >= 18)
    {
        printf("MAIOR de idade.");
    } else {
        printf("MENOR de idade.");
    }
    return 0;
}